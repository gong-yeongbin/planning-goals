---
name: planning-goals
description: Use when the user gives a rough one-line goal and wants it turned into something to feed Claude Code's /goal — phrases like "goal 정리해 줘", "goal 조건 만들어 줘", "/goal 에 넣을 조건", "/planning-goals <목표>", or when a goal condition would be too vague for the /goal evaluator to judge.
---

# planning-goals

## 원리

`/goal <조건>` 의 평가 모델은 명령을 실행하지 못하고 Claude 의 출력만 읽는다. 따라서 조건은 출력에서 확인 가능한 상태여야 하고, 단계마다 증거(명령 출력)를 남겨야 한다. 작업 계획은 파일에, 조건문은 그 파일을 가리키는 한 문장으로 분리한다.

## 결과물

이 스킬이 끝나면 다음 두 가지가 존재한다. 다른 형태의 출력은 없다.

1. `<cwd>/.goal/<슬러그>.md` — 아래 템플릿 그대로
2. 사용자에게 보여 주는 마지막 메시지: 파일 경로 + 붙여 넣을 `/goal ...` 한 줄

## 절차

1. **조사.** 코드·문서·로그로 알 수 있는 것은 읽어서 채운다. 묻지 않는다.
2. **질문 루프.** 조사로 채울 수 없는 것만 묻는다. 한 번에 질문 하나. 질문마다 추천 답을 같이 적는다. 모호한 답("대충", "나중에")은 파고든다. 다음 세 가지가 채워지면 멈춘다.
   - 끝난 상태가 무엇인지
   - 각 단계를 무엇으로 증명하는지 (명령, 파일, 출력)
   - 이번에 하지 않을 것
3. **파일 작성.** `.goal/<슬러그>.md` 를 템플릿대로 쓴다. `.goal/` 이 없으면 만든다.
4. **마무리.** 파일 경로와 `/goal` 한 줄을 보여 주고 멈춘다. `/goal` 은 슬래시 명령이라 사용자가 입력한다.

## 파일 템플릿

```markdown
# Goal: <목표 한 줄>
Date: <ISO date>

## 완료 조건
<이 문장을 그대로 /goal 뒤에 붙여 넣음>

## 범위 밖
- <하지 않을 것>

## 단계
### 1. <단계 이름>
- 동작: <무엇을 어떻게 한다>
- 검증: <어떤 명령이나 확인으로 끝났음을 보인다>
- [ ] 완료
```

## 완료 조건 문장 규칙

- 파일을 가리킨다: `.goal/<슬러그>.md 의 모든 단계가 [x] 완료로 표시되고 각 단계의 검증 결과가 출력에 있음`
- 필요하면 실행 금지 항목을 한 줄 덧붙인다: `git 은 실행하지 않고 명령만 제시`
- 4000자 이하. 동작·순서·이유는 조건문이 아니라 파일의 단계 절에 둔다.

## 예

`/planning-goals 헬스체크 엔드포인트 추가` → 조사: 라우터 파일 위치, 테스트 러너, 서버 포트를 코드에서 확인. 질문 1개: "응답에 DB 연결 상태도 넣을까요? (추천: 아니오, 200 OK 만. DB 상태는 범위 밖)".

`.goal/healthcheck.md`:

```markdown
# Goal: 헬스체크 엔드포인트 추가
Date: 2026-09-17

## 완료 조건
.goal/healthcheck.md 의 모든 단계가 [x] 완료로 표시되고 각 단계의 검증 결과가 출력에 있음. git 은 실행하지 않고 명령만 제시.

## 범위 밖
- DB 연결 상태 확인 (다음 목표로)

## 단계
### 1. 엔드포인트 구현
- 동작: `GET /health` 가 `{"status":"ok"}` 를 200 으로 돌려주도록 라우터에 추가하고 테스트 1개 작성
- 검증: `npm test` 전부 통과 — 출력 붙여 넣기
- [ ] 완료

### 2. 실행 확인
- 동작: 서버를 띄우고 `curl -s -o /dev/null -w '%{http_code}' localhost:3000/health`
- 검증: 출력이 `200` — 출력 붙여 넣기
- [ ] 완료

### 3. 커밋 명령 제시
- 동작: `git add`/`git commit` 을 코드 블록으로
- 검증: 코드 블록이 출력에 있음
- [ ] 완료
```

마지막 메시지:

```
.goal/healthcheck.md 를 만들었습니다.
/goal .goal/healthcheck.md 의 모든 단계가 [x] 완료로 표시되고 각 단계의 검증 결과가 출력에 있음. git 은 실행하지 않고 명령만 제시.
```

## 흔한 실수

| 실수 | 대신 |
|---|---|
| 조건문 하나에 작업·동작·제약을 전부 넣음 | 조건문은 파일을 가리키는 한 문장, 나머지는 단계 절 |
| 결과물을 다 만든 뒤 질문 여러 개를 한꺼번에 | 파일 쓰기 전에 하나씩, 추천 답과 함께 |
| "안정화", "완성" 같은 상태를 조건으로 씀 | 출력으로 보일 수 있는 것: 명령 출력, 파일 존재, 특정 문자열 |
| 검증 없는 단계 | 단계마다 검증 한 줄, 없으면 단계를 쪼개거나 범위 밖으로 |
