---
name: planning-goals
description: Use when the user gives a rough one-line goal and wants it turned into something to feed Claude Code's /goal — phrases like "goal 정리해 줘", "goal 조건 만들어 줘", "/goal 에 넣을 조건", "/planning-goals <목표>", or when a goal condition would be too vague for the /goal evaluator to judge.
---

# planning-goals

## 원리

`/goal <조건>` 의 평가 모델은 명령을 실행하지 못하고 Claude 의 출력만 읽는다. 따라서 조건은 출력에서 확인 가능한 상태여야 하고, 단계마다 증거(명령 출력)를 남겨야 한다. 작업 계획은 파일에, 조건문은 그 파일을 가리키는 한 문장으로 분리한다. 파일은 루프 중 Claude 가 매 턴 읽는 유일한 계획서이므로, 조사한 사실은 근거와 함께 적어 다시 조사하지 않게 한다. 만드는 일과 맞는지 판정하는 일은 goal 을 둘로 나눈다. 구현 goal 은 「만들어졌고 응답한다」까지, TEST goal 은 「동작이 기대와 같다」를 판정한다.

## 결과물

이 스킬이 끝나면 다음 세 가지가 존재한다. 다른 형태의 출력은 없다.

1. `<cwd>/.goal/<슬러그>.md` — 구현 goal. 아래 템플릿대로
2. `<cwd>/.goal/<슬러그>-test.md` — TEST goal. 아래 TEST 템플릿대로
3. 사용자에게 보여 주는 마지막 메시지: 파일 경로 2개 + 붙여 넣을 `/goal ...` 두 줄 (구현 → TEST 순서)

## 절차

1. **조사.** 코드·문서·로그로 알 수 있는 것은 읽어서 채운다. 묻지 않는다. 사실마다 근거를 남긴다: `확인함 — <명령 또는 파일:행>`. 근거 없이 짐작한 것은 `미확인` 으로 표시하고, 질문으로 돌리거나 단계 1 「확인」으로 넣는다.
2. **질문 루프.** 조사로 채울 수 없는 것만 묻는다. 한 번에 질문 하나. 질문마다 추천 답을 같이 적는다. 모호한 답("대충", "나중에")은 파고든다. 다음 세 가지가 채워지면 멈춘다.
   - 끝난 상태가 무엇인지
   - 각 단계를 무엇으로 증명하는지 (명령, 파일, 출력)
   - 이번에 하지 않을 것
3. **파일 작성.** `.goal/<슬러그>.md` 와 `.goal/<슬러그>-test.md` 를 템플릿대로 쓴다. `.goal/` 이 없으면 만든다. TEST 케이스는 조사에서 나온 분기(정상, 오류 코드, 조건, 경계값)마다 하나씩 뽑는다. 분기를 모르면 조사로 돌아간다.
4. **마무리.** 파일 경로 2개와 `/goal` 두 줄을 보여 주고 멈춘다. `/goal` 은 슬래시 명령이라 사용자가 입력한다. 구현 goal 이 끝난 뒤 TEST goal 을 입력한다.

## 파일 템플릿

필수 절: 완료 조건, 만들 것, 범위 밖, 단계, DoD. 선택 절은 아래 조건에 맞을 때만 쓴다.

```markdown
# Goal: <목표 한 줄>
Date: <ISO date>

## 완료 조건
<이 문장을 그대로 /goal 뒤에 붙여 넣음>

## 만들 것
<무엇을, 어떤 방식으로, 무엇은 바꾸지 않는지. 2~3문장>

## 참조
- <읽어야 할 문서·이슈·파일>

## 결정
- <고른 방식과 버린 방식. 루프 중 다시 논의하지 않음>

## 조사 결과
| 항목 | 값 | 근거 |
|---|---|---|
| <사실> | <값> | 확인함 — <명령 또는 파일:행> |

## 범위 밖
- <하지 않을 것> — <이유 한 줄>

## 제약
- <하는 동안 깨지면 안 되는 것> — <이유>

## 단계
### 1. <단계 이름>
- 동작: <무엇을 어떻게 한다>
- 검증: <어떤 명령이나 확인으로 끝났음을 보인다>
- [ ] 완료

## DoD
- [ ] <도착 상태> — 검증: <명령 또는 확인>

## 변경 기록
- <날짜> <무엇이 왜 바뀌었나. 원인 한 줄>
```

선택 절을 쓰는 조건:

| 절 | 쓰는 조건 |
|---|---|
| 참조 | 조사에서 읽은 문서·이슈·명세가 있을 때 |
| 결정 | 목표를 이루는 방법이 둘 이상이고 하나를 골랐을 때 |
| 조사 결과 | 근거 있는 사실이 3개 이상일 때 |
| 제약 | 실서버 호출 금지, 특정 파일 수정 금지처럼 지켜야 할 것이 있을 때 |
| 변경 기록 | 처음엔 쓰지 않는다. 루프 중 계획이 틀린 것이 드러나면 Claude 가 이 절을 만들어 덧붙인다 |

## TEST 파일 템플릿

구현 goal 이 끝난 뒤 따로 도는 goal 이다. 판정만 하고 코드는 고치지 않는다. 실패는 결과 절에 기록하고 멈춘다. 고치는 일은 구현 goal 을 다시 열어서 한다.

```markdown
# TEST: <목표 한 줄>
Date: <ISO date>
대상: .goal/<슬러그>.md

## 완료 조건
<이 문장을 그대로 /goal 뒤에 붙여 넣음>

## 사전 조건
- <구현 goal 완료, 서버 실행 등. 확인 명령 포함>

## 제약
- 코드 수정 금지 — 실패는 기록만 한다
- <부수효과가 있는 케이스와 그 대처>

## 케이스
| # | 분기 | 입력 | 실행 | 기대 | 판정 |
|---|---|---|---|---|---|
| 1 | <정상/오류 이름> | <파라미터> | <명령> | <출력에 있어야 할 것> | [ ] |

## 결과
- <케이스 #> 통과/실패 — <출력 한 줄 요약>

## 판정
- [ ] 전체 통과 — 검증: 결과 절에 실패 0건
- 실패 시: <원인 후보 한 줄, 구현 goal 의 변경 기록으로 넘김>
```

케이스 규칙:

- 정상 1개 이상, 오류 분기마다 1개, 경계값이 있으면 1개
- 실행은 명령 한 줄. 사람이 화면을 봐야 하는 케이스는 「사전 조건」이 아니라 「제약」에 "사용자 확인"으로 적고 판정에서 뺀다
- 부수효과(상태를 바꾸는 호출)가 있으면 그 케이스를 마지막에 두거나 되돌리는 명령을 실행 열에 붙인다

## 완료 조건 문장 규칙

- 구현: `.goal/<슬러그>.md 의 모든 단계와 DoD 가 [x] 완료로 표시되고 각 항목의 검증 결과가 출력에 있음`
- TEST: `.goal/<슬러그>-test.md 의 모든 케이스가 판정되어 [x] 또는 실패로 표시되고 각 케이스의 실행 출력이 있으며 판정 절이 채워짐. 코드는 수정하지 않음`
- 필요하면 실행 금지 항목을 한 줄 덧붙인다: `git 은 실행하지 않고 명령만 제시`
- 4000자 이하. 동작·순서·이유는 조건문이 아니라 파일에 둔다.

## 단계와 DoD 의 구분

- 단계는 가는 길, DoD 는 도착 상태. DoD 는 1~3개로 짧게 쓰고 항목마다 검증을 붙인다.
- 마지막 단계는 「DoD 검증」으로 두어 DoD 의 검증 명령을 한 번에 실행하고 출력을 남긴다.
- 단계와 DoD 가 같은 말을 하면 DoD 쪽을 지운다.
- 동작이 기대와 같은지는 DoD 에 넣지 않는다. TEST 파일의 케이스로 옮긴다.

## 예

`/planning-goals 헬스체크 엔드포인트 추가` → 조사: 라우터 파일 위치, 테스트 러너, 서버 포트를 코드에서 확인. 질문 1개: "응답에 앱 버전도 넣을까요? (추천: 아니오, status 만. 버전은 범위 밖)".

`.goal/healthcheck.md`:

```markdown
# Goal: 헬스체크 엔드포인트 추가
Date: 2026-09-17

## 완료 조건
.goal/healthcheck.md 의 모든 단계와 DoD 가 [x] 완료로 표시되고 각 항목의 검증 결과가 출력에 있음. git 은 실행하지 않고 명령만 제시.

## 만들 것
`GET /health` 가 `{"status":"ok"}` 를 200 으로 돌려준다. 기존 라우터 파일에 라우트 하나와 테스트 하나를 더한다. 다른 라우트와 응답 형식은 바꾸지 않는다.

## 조사 결과
| 항목 | 값 | 근거 |
|---|---|---|
| 라우터 파일 | `src/routes.js` | 확인함 — `grep -rn "app.get" src/` |
| 테스트 러너 | jest, `npm test` | 확인함 — `package.json` scripts.test |
| 서버 포트 | 3000 | 확인함 — `src/server.js:12` |

## 범위 밖
- 응답에 앱 버전 포함 — 다음 목표로

## 제약
- 기존 라우트 파일 외 수정 금지 — `git status --short` 에 `src/routes.js`, `test/health.test.js` 만 나와야 함

## 단계
### 1. 엔드포인트 구현
- 동작: `src/routes.js` 에 라우트 추가, `test/health.test.js` 작성
- 검증: `npm test` 전부 통과 — 출력 붙여 넣기. `git status --short` 출력 붙여 넣기
- [ ] 완료

### 2. 커밋 명령 제시
- 동작: `git add`/`git commit` 을 코드 블록으로
- 검증: 코드 블록이 출력에 있음
- [ ] 완료

### 3. DoD 검증
- 동작: 서버를 띄우고 DoD 의 검증 명령 실행
- 검증: 아래 DoD 항목의 출력 붙여 넣기
- [ ] 완료

## DoD
- [ ] `GET /health` 가 200 — 검증: `curl -s -o /dev/null -w '%{http_code}' localhost:3000/health` 출력이 `200`
```

`.goal/healthcheck-test.md`:

```markdown
# TEST: 헬스체크 엔드포인트 추가
Date: 2026-09-17
대상: .goal/healthcheck.md

## 완료 조건
.goal/healthcheck-test.md 의 모든 케이스가 판정되어 [x] 또는 실패로 표시되고 각 케이스의 실행 출력이 있으며 판정 절이 채워짐. 코드는 수정하지 않음.

## 사전 조건
- 서버 실행 중 — `curl -s -o /dev/null -w '%{http_code}' localhost:3000/` 가 응답

## 제약
- 코드 수정 금지 — 실패는 기록만 한다

## 케이스
| # | 분기 | 입력 | 실행 | 기대 | 판정 |
|---|---|---|---|---|---|
| 1 | 정상 | GET /health | `curl -s -i localhost:3000/health` | 첫 줄 `200`, 본문 `{"status":"ok"}` | [ ] |
| 2 | 메서드 불일치 | POST /health | `curl -s -o /dev/null -w '%{http_code}' -X POST localhost:3000/health` | `404` 또는 `405` | [ ] |
| 3 | 기존 라우트 영향 없음 | GET / | `curl -s -o /dev/null -w '%{http_code}' localhost:3000/` | 구현 전과 같은 코드 | [ ] |

## 결과
- (루프가 채움)

## 판정
- [ ] 전체 통과 — 검증: 결과 절에 실패 0건
```

마지막 메시지:

```
.goal/healthcheck.md 와 .goal/healthcheck-test.md 를 만들었습니다. 구현이 끝난 뒤 TEST 를 입력하세요.
/goal .goal/healthcheck.md 의 모든 단계와 DoD 가 [x] 완료로 표시되고 각 항목의 검증 결과가 출력에 있음. git 은 실행하지 않고 명령만 제시.
/goal .goal/healthcheck-test.md 의 모든 케이스가 판정되어 [x] 또는 실패로 표시되고 각 케이스의 실행 출력이 있으며 판정 절이 채워짐. 코드는 수정하지 않음.
```

## 흔한 실수

| 실수 | 대신 |
|---|---|
| 조건문 하나에 작업·동작·제약을 전부 넣음 | 조건문은 파일을 가리키는 한 문장, 나머지는 파일 |
| 결과물을 다 만든 뒤 질문 여러 개를 한꺼번에 | 파일 쓰기 전에 하나씩, 추천 답과 함께 |
| "안정화", "완성" 같은 상태를 조건으로 씀 | 출력으로 보일 수 있는 것: 명령 출력, 파일 존재, 특정 문자열 |
| 검증 없는 단계나 DoD | 항목마다 검증 한 줄, 없으면 쪼개거나 범위 밖으로 |
| 근거 없는 조사 결과 | `확인함 — 명령/파일:행` 이 없으면 `미확인` 으로 적고 확인 단계로 |
| 범위 밖에 이유가 없음 | grep 에는 잡히지만 대상이 아닌 것처럼, 이유를 붙여 루프가 범위를 다시 넓히지 않게 |
| 조사 근거 설명을 문단으로 씀 | 한 줄. 파일은 매 턴 읽히므로 길이가 비용 |
| 구현 goal 의 DoD 에서 동작까지 판정 | 구현은 「만들어졌고 응답한다」까지. 동작은 TEST 파일의 케이스 |
| TEST goal 에서 코드를 고침 | 기록만 하고 멈춘다. 고치는 건 구현 goal 을 다시 열어서 |
