---
name: planning-goals
description: Turns a rough one-line goal into two plan files (.goal/<slug>.md for building, .goal/<slug>-test.md for judging) plus the exact `/goal ...` lines to paste into Claude Code, so the /goal evaluator can actually judge completion from Claude's output. Use whenever the user mentions /goal, says "goal 정리해 줘", "goal 조건 만들어 줘", "/goal 에 넣을 조건", "/planning-goals <목표>", wants Claude to keep working until something is done, or states a goal like "X 완성", "X 안정화", "X 전환" that an evaluator could not judge from output alone.
---

# planning-goals

## 왜 이렇게 하나

`/goal <조건>` 의 평가 모델은 매 턴 끝에 대화만 읽고 충족·미충족·불가능을 판정한다. 명령을 실행하지도, 파일을 읽지도 못한다. 그래서 "안정화됨", "완성됨" 같은 조건은 판정할 수 없고, "`npm test` 출력에 FAIL 없음" 처럼 Claude 가 대화에 남긴 출력으로 보이는 조건만 판정할 수 있다. 조건은 4,000자까지라 작업 내용을 다 넣을 수도 없다. 자세한 동작은 `references/goal-evaluator.md` 에 있다.

이 스킬은 그 제약에 맞춰 세 가지를 한다.

- **계획은 파일에, 조건은 한 문장으로.** 조건문은 파일을 가리키기만 하고, 단계·검증·범위·제약은 파일에 둔다. 루프 중 Claude 는 매 턴 이 파일을 읽으므로 파일이 곧 지침이다.
- **만드는 goal 과 판정하는 goal 을 나눈다.** 구현 goal 은 "만들어졌고 응답한다" 까지만, TEST goal 은 "동작이 기대와 같다" 를 판정한다. 하나에 섞으면 조건이 길어지고, 구현하던 Claude 가 자기 결과를 관대하게 채점한다.
- **조사한 사실에 근거를 붙인다.** 근거 없는 사실은 루프가 매 턴 다시 조사하거나 틀린 가정 위에서 일한다.

## 결과물

스킬이 끝나면 정확히 이 셋이 있다. 채팅에 계획을 풀어 놓는 것은 결과물이 아니다.

1. `<cwd>/.goal/<슬러그>.md` — 구현 goal. `assets/goal.md` 를 채운다
2. `<cwd>/.goal/<슬러그>-test.md` — TEST goal. `assets/goal-test.md` 를 채운다
3. 마지막 메시지 — 파일 경로 2개와 붙여 넣을 `/goal ...` 두 줄 (구현 → TEST 순서)

슬러그는 영문 소문자와 하이픈으로 짓는다 (`add-health-endpoint`, `migrate-config-yaml`). 한글 목표는 뜻을 옮긴다. `/goal` 은 슬래시 명령이라 사용자가 직접 입력하므로 마지막 메시지에서 멈춘다.

전체 예는 `references/example.md` 에 있다. 처음 쓰는 프로젝트 유형이면 그 형태를 그대로 따른다.

## 절차

### 1. 조사

코드·문서·로그로 알 수 있는 것은 읽어서 채운다. 알 수 있는 것을 묻지 않는다.

사실마다 근거를 남긴다: `확인함 — <명령 또는 파일:행>`. 실행 로그 파일도 근거다 (`확인함 — data/app.log:80`). 근거 없이 짐작한 것은 `미확인` 으로 표시하고, 질문으로 돌리거나 단계 1 「확인」으로 넣는다. 실서버 호출처럼 금지된 행동으로만 확인되는 사실은 `미확인 — <이유>` 로 두고 확인 단계로 돌리지 않는다. TEST 에서 그 분기는 판정 불가가 된다. 근거 설명은 한 줄이다. 파일은 매 턴 읽히므로 길이가 곧 비용이다.

조사 결과 목표가 이미 거의 끝나 있는 경우가 자주 있다. 그럴 때는 "완성" 을 되묻지 말고, 남은 항목 후보(미커밋 변경, 보류된 리뷰 항목, 실데이터로 확인 안 된 것)를 조사에서 뽑아 첫 질문에서 고르게 한다.

### 2. 질문

조사로 채울 수 없는 것만 묻는다. 한 번에 하나. 질문마다 추천 답을 붙인다. 추천 답이 있으면 사용자는 고개만 끄덕이면 되고, 결과물을 다 만든 뒤에 질문을 몰아 넣으면 사용자가 파일을 다시 고쳐야 한다.

"대충", "나중에" 같은 답은 파고든다. 다음 셋이 채워지면 멈춘다.

- 끝난 상태가 무엇인지
- 각 단계를 무엇으로 증명하는지 (명령, 파일, 출력)
- 이번에 하지 않을 것

Claude 가 정할 수 있는 것(구현 위치, 테스트 함수 이름)은 묻지 말고 「결정」 절에 적는다. 사용자 선호가 갈리는 것(범위, 무엇을 뺄지)만 묻는다. 커밋 여부는 묻지 않는다. 루프 안에서 git 은 실행하지 않고 명령만 제시하는 것이 고정 규칙이며, 커밋은 사용자가 결과를 보고 직접 한다.

### 3. 파일 작성

`.goal/` 이 없으면 만든다. 파일 범위를 제약으로 걸 때는 조사에서 `git status --short` 출력을 찍어 조사 결과에 넣고, 제약은 "그 스냅샷 대비 추가 변경이 <파일들>뿐" 으로 쓴다. 프로젝트에 원래 있던 미커밋 변경이나 미추적 파일(`.goal/` 포함)이 검증을 깨뜨리지 않게 하는 방법이다.

**구현 goal** (`assets/goal.md`). 필수 절은 완료 조건, 만들 것, 범위 밖, 단계, DoD. 선택 절은 조건에 맞을 때만 쓴다. 작은 목표에 빈 절을 늘어놓으면 루프가 읽을 게 늘 뿐이다.

| 선택 절 | 쓰는 조건 |
|---|---|
| 참조 | 조사에서 읽은 문서·이슈·명세가 있을 때 |
| 결정 | 목표를 이루는 방법이 둘 이상이고 하나를 골랐을 때 |
| 조사 결과 | 근거 있는 사실이 3개 이상일 때. 행 수에 상한은 없다. 큰 작업은 사실도 많고, 빠진 사실은 루프가 다시 조사한다. 대신 한 행은 한 줄이다 |
| 제약 | 실서버 호출 금지, 특정 파일 수정 금지처럼 하는 동안 깨지면 안 되는 것이 있을 때. 부수효과가 없다고 근거로 확인된 읽기 전용 명령은 루프에서 실행해도 되며, 여기에 `실행 허용: <명령> — <근거>` 로 적는다 |
| 변경 기록 | 처음엔 쓰지 않는다. 루프 중 계획이 틀린 것이 드러나면 Claude 가 만들어 덧붙인다. 원인 한 줄을 요구하는 이유는 그게 다음 계획의 조사 항목이 되기 때문이다 |

범위 밖 항목에는 이유를 붙인다. grep 에는 잡히지만 실제로는 대상이 아닌 것처럼, 이유가 없으면 루프가 범위를 다시 넓힌다.

단계는 가는 길, DoD 는 도착 상태다. 단계마다 동작·검증·완료 슬롯을 채우고, 검증이 없는 단계는 쪼개거나 범위 밖으로 보낸다. DoD 는 1~3개로 짧게 쓰고 항목마다 검증을 붙이며, 마지막 단계를 「DoD 검증」으로 두어 그 명령들을 한 번에 실행하고 출력을 남긴다. 단계와 DoD 가 같은 말을 하면 DoD 쪽을 지운다. 동작이 기대와 같은지는 DoD 에 넣지 않고 TEST 파일로 보낸다.

**TEST goal** (`assets/goal-test.md`). 구현 goal 이 끝난 뒤 따로 도는 goal 이다. 판정만 하고 코드는 고치지 않는다. 실패는 결과 절에 기록하고 멈추며, 고치는 일은 구현 goal 을 다시 열어서 한다. 이렇게 나눠야 TEST 가 "고치면서 통과시키는" 루프가 되지 않는다.

케이스는 조사에서 나온 분기마다 하나씩 뽑는다. 정상 1개 이상, 오류 분기마다 1개, 경계값이 있으면 1개. 분기를 모르면 조사로 돌아간다. 오류 분기가 없는 목표(문서 수정 등)는 정상 케이스와 "다른 파일에 영향 없음" 케이스로 끝낸다. 억지로 오류 케이스를 만들지 않는다. 그런 케이스는 판정할 게 없어 루프가 통과시키는 데만 시간을 쓴다.

케이스 하나의 실행은 명령 한 줄이고, 기대는 출력에서 찾을 문자열이다. 단위 테스트 실행(`go test -run X`, `npm test -- -t X`)도 케이스의 실행으로 쓴다. 구현 goal 이 새로 쓰는 테스트라면 「결정」 절에 함수 이름을 고정해 TEST 가 미리 가리킬 수 있게 한다. 한 테스트 함수가 여러 단언을 묶고 있으면 케이스도 하나로 두고 기대에 "실패 문자열 없음" 을 적는다.

- 사람이 화면을 봐야 하는 항목은 케이스가 아니라 「제약」의 "사용자 확인" 으로 적고 판정에서 뺀다
- 부수효과(상태를 바꾸는 호출)가 있는 케이스는 마지막에 두거나 되돌리는 명령을 실행 열에 붙인다
- 사전 조건이 안 맞아 실행할 수 없는 케이스는 "판정 불가" 로 기록한다. [x] 와 실패만 있으면 루프가 억지로 둘 중 하나를 고른다
- 실행 열은 마크다운 표 안이므로 `|` 는 `\|` 로 쓴다. `&&` 로 이은 복합 명령은 허용하되 한 줄로. 임시 디렉터리나 설정 파일 같은 준비가 필요하면 준비 명령은 「사전 조건」에 적고 실행 열은 짧게 유지한다

### 4. 마무리

파일 경로 2개와 `/goal` 두 줄을 보여 주고 멈춘다. 구현 goal 이 끝난 뒤 TEST goal 을 입력하라고 한 줄 덧붙인다.

## 완료 조건 문장

- 구현: `.goal/<슬러그>.md 의 모든 단계와 DoD 가 [x] 완료로 표시되고 각 항목의 검증 결과가 출력에 있음. git 은 실행하지 않고 명령만 제시`
- TEST: `.goal/<슬러그>-test.md 의 모든 케이스가 [x]·실패·판정 불가 중 하나로 표시되고 각 케이스의 실행 출력이 있으며 판정 절이 채워짐. 코드는 수정하지 않음`
- 구현 조건에는 항상 `git 은 실행하지 않고 명령만 제시` 를 덧붙인다. 다른 실행 금지 항목이 있으면 같은 줄에 잇는다: `git 과 실서버 호출은 실행하지 않고 명령만 제시`
- 동작·순서·이유는 조건문이 아니라 파일에 둔다. 조건문에 작업을 넣기 시작하면 4,000자에 닿고 평가 모델이 무엇을 볼지 흐려진다

## 자주 틀리는 것

| 증상 | 대신 |
|---|---|
| 채팅에 계획을 다 풀어 놓고 파일은 안 만듦 | 결과물은 파일 둘과 `/goal` 두 줄뿐 |
| 조건문에 작업·동작·제약을 전부 넣음 | 조건문은 파일을 가리키는 한 문장 |
| 결과물을 다 만든 뒤 질문 여러 개를 한꺼번에 | 파일 쓰기 전에 하나씩, 추천 답과 함께 |
| "안정화", "완성" 을 조건으로 씀 | 출력으로 보이는 것: 명령 출력, 파일 존재, 특정 문자열 |
| 근거 없는 조사 결과 | `확인함 — 명령/파일:행`, 없으면 `미확인` |
| 구현 goal 의 DoD 에서 동작까지 판정 | 동작은 TEST 파일의 케이스 |
| TEST goal 에서 코드를 고침 | 기록만 하고 멈춘다 |
